{
  "name": "@vulndesk/core",
  "version": "0.1.0",
  "description": "Headless, framework-agnostic CVE/advisory core for Vulndesk: CVE5 record validation (AJV) and transforms shared by the web UI, the standalone bundle, and the MCP server.",
  "license": "MIT",
  "main": "src/index.js",
  "files": [
    "src",
    "schema"
  ],
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
